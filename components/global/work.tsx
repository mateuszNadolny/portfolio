import WorkCardContainer from "@/components/global/work-card-container";

const Work = () => {
  return (
    <section
      id="work"
      className="min-h-screen w-full flex justify-center py-16"
    >
      <WorkCardContainer />
    </section>
  );
};

export default Work;
